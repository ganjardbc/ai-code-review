# Environment Specification

## Purpose
Document required execution environment dependencies.

## Specifications
* **Runtime**: Node.js v22.x LTS.
* **Process Manager**: Docker with PM2 or node direct executions.
* **System Utilities**: Git CLI binary (v2.40+) must be installed and available in the worker container's `$PATH`.
